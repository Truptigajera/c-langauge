#include<stdio.h>
#include<conio.h>
void main()
{
	float total,per,maths,eng,sci;
	clrscr();
	printf("enter maths,eng,sci:");
	scanf("%f%f%f",&maths,&sci,&eng);
	total=maths+sci+eng;
	if(maths<35 || sci<35 || eng<35)
	{
	printf("you are fail:");
	}
	else{
	per=total/300*100;
	printf("percentage is:%f",per);

	if(per>=75)
	{
	printf("A:");
	}
	else if(per>=60)
	{
	printf("B:");
	}
	else if(per>=45)
	{
	printf("C:");
	}
	else if(per>=35)
	{
	printf("D:");
	}
	}
	getch();
}